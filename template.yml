parameters:
  - name: scenario
    type: string
    default: 'all'
  - name: executeScenario
    type: boolean
    default: true

steps:
  - script: echo "Scenario - ${{ parameters.scenario }}"
    displayName: "Execute on branches: ${{ parameters.scenario }}"
    condition: eq(${{ parameters.executeScenario }}, true)
