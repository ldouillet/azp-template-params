# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  planet: Hoth
  fight: true
  # mood: motivated

steps:
  # - template: template.yml
  # - template: template.yml
  #   parameters:
  #     shipName: X-wing
  # - template: template.yml
  #   parameters:
  #     planet: $(planet)
  - template: template.yml
  - template: template.yml
    parameters:
      # fight: ${{ variables.fight }}
      fight: $(fight)
  - template: template.yml
  - template: template.yml
    parameters:
      planet: $(planet)
      fight: ${{ variables.fight }}
      # fight: $(fight)
  # - template: template.yml
  #   parameters:
  #     fight: eq(variables.mood, 'motivated')
