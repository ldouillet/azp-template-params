parameters:
  - name: shipName
    type: string
    default: Millennium Falcon
  - name: planet
    type: string
    default: Tatooine

steps:
  - script: echo "${{ parameters.shipName }} is flying to ${{ parameters.planet }}"
