parameters:
  - name: scenario
    type: string
    default: 'Do it'
  - name: executeScenario
    type: boolean
    default: true

steps:
  - script: echo "Scenario - ${{ parameters.scenario }}"
    condition: eq(${{ parameters.executeScenario }}, true)
